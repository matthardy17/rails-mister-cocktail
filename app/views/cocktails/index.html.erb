<h1>All Cocktails</h1>

<% @cocktails.each do |cocktail| %>
<p><%= link_to cocktail.name, cocktail_path(cocktail) %></p>
<% end %>


<%= link_to "Add a new cocktail", new_cocktail_path, class: "btn btn-primary" %>

<%# add form here to add cocktail %>

<%# two main pages, index with all cocktails where you can add a cocktail, and then cocktail show page where you add the doses/ingredients %>